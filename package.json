{
  "name": "thom-aws-auth",
  "version": "0.0.1",
  "description": "lambda functions for auth with dynamodb",
  "main": "index.js",
  "scripts": {
    "build": "node esbuild.build.js",
    "clean": "scripts/clean.sh",
    "apply": "npm run build && cd terraform/aws && terraform init && terraform apply -auto-approve",
    "destroy": "cd terraform/aws && terraform destroy -auto-approve && npm run clean",
    "up": "cd terraform/docker && terraform init && terraform apply -auto-approve",
    "down": "cd terraform/docker && terraform destroy -auto-approve",
    "migrate": "scripts/migrateLocalDB.sh"
  },
  "author": "Thomas Newman",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1113.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.93",
    "@types/node": "^17.0.23",
    "esbuild": "^0.14.36",
    "typescript": "^4.6.3"
  }
}
